# Optional Docker Compose overrides.
# Copy to `.env` and adjust if needed.

# TeX Live image tag
TL_YEAR=latest

# TeX Live package snapshot date (YYYY/MM/DD)
SNAPSHOT_DATE=2025/09/01

# Container user metadata
DOCKER_USER=latex
UID=1000
GID=1000
